apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: {{ .Values.name | required "Please provide a name for the service you want to deploy" }}
  name: {{ .Values.name }}
  annotations:
    {{- if .Values.pod.annotations }}
    {{- toYaml .Values.pod.annotations | nindent 4 }}
    {{- end }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.name }}
    spec:
      {{- if .Values.pod.podOptions -}}
      {{- toYaml .Values.pod.podOptions | nindent 6 }}
      {{- end }}
      volumes:
        {{- range .Values.storage.fromClaims }}
        - name: {{ .name }}
          persistentVolumeClaim:
            claimName: {{ .claimName }}
        {{- end }}
        {{- if .Values.storage.extraVolumes }}
          {{- toYaml .Values.storage.extraVolumes | nindent 8 }}
        {{- end }}
      {{- if .Values.pod.securityContext }}
      securityContext:
        {{- toYaml .Values.pod.securityContext | nindent 8 }}
      {{- end }}
      containers:
        - image: {{ .Values.pod.image | required "Please provide an image for the service you want to deploy" }}
          name: {{ .Values.name }}
          {{- if .Values.pod.command }}
          command:
            {{- toYaml .Values.pod.command | nindent 12 }}
          {{- end }}
          {{- if .Values.pod.args }}
          args:
            {{- toYaml .Values.pod.args | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.pod.resources | nindent 12 }}
          {{- if .Values.pod.probes.livenessProbe }}
          livenessProbe:
            {{- toYaml .Values.pod.probes.livenessProbe | nindent 12 }}
          {{- end }}
          {{- if .Values.pod.probes.readinessProbe }}
          readinessProbe:
            {{- toYaml .Values.pod.probes.readinessProbe | nindent 12 }}
          {{- end }}
          ports:
            - containerPort: {{ .Values.pod.ports.servicePort }}
              protocol: TCP
            {{- range .Values.pod.ports.extraPorts}}
            - containerPort: {{ .port }}
              protocol: {{ .protocol | default "TCP" }}
              name: {{ .name }}
            {{- end }}
            {{- range .Values.pod.ports.hostPorts }}
            - containerPort: {{ .port }}
              protocol: {{ .protocol | default "TCP" }}
              name: {{ .name }}
              hostPort: {{ .port }}
            {{- end }}
          {{- if .Values.pod.volumeMounts }}
          volumeMounts:
            {{- toYaml .Values.pod.volumeMounts | nindent 12 }}
          {{- end }}
          envFrom:
            - configMapRef:
                name: {{ .Values.name }}-config
            {{- if .Values.pod.environmentData.envSecretName }}
            - secretRef:
                name: {{ .Values.pod.environmentData.envSecretName }}
            {{- end }}
          env:
            {{- range .Values.pod.environmentData.envSecrets }}
            - name: {{ .name }}
              valueFrom:
                secretKeyRef:
                  name: {{ .secretName }}
                  key: {{ .key }}
            {{- end }}
