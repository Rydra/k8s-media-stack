apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: {{ .Values.service.name }}-db
  name: {{ .Values.service.name }}-db
spec:
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.dbServicePort }}
      targetPort: {{ .Values.service.dbServicePort }}
  selector:
    app.kubernetes.io/name: {{ .Values.service.name }}-db
