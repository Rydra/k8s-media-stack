apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.service.name }}-config
data:
  TORRENTING_PORT: "{{ .Values.service.torrentingPort }}"
  WEBUI_PORT: "{{ .Values.service.servicePort }}"
  {{ if .Values.service.env }}
  {{- toYaml .Values.service.env | nindent 2 }}
  {{- end }}
